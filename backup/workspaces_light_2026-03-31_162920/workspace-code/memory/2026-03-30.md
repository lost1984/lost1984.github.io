
- 用户明确纠偏：目标不是只做站点匿名接口探测，而是“登录这些站点，拿到用户实际账号下的 key，再用真实 key 测模型和可用性”；后续默认按“先执行、后汇报、不先问，除非权限/目标/风险卡住”推进。
- 基于现有站名到真实域名映射，已实测确认两站具备真实 API 网关特征：`冰の公益` 对应 `https://ice.v.ua`，`97公益站` 高概率对应 `https://api.mmkg.cloud`；两站 `GET /v1/models` 均返回鉴权错误（分别为 `API_KEY_REQUIRED` 与 `未提供令牌`），说明模型接口真实存在但需要用户 key/token。
- `ice.v.ua` 前端公开配置再次确认：`api_base_url` 为 `https://ice.v.ua`，`linuxdo_oauth_enabled=true`，存在自定义菜单 `签到/重置`（id `f8c961a6027f9cb0`，URL `https://signv.ice.v.ua/embed`）、`延迟监测`、`实时号池`（`https://pool.ice.v.ua/`）。
- 已将阶段性站点结果沉淀到工作区文件 `/root/.openclaw/workspace-code/linuxdo_oauth_sites_summary.md`，其中记录了 `ice.v.ua` 与 `api.mmkg.cloud` 的已验证事实、模型接口状态、签到能力和当前阻塞。
- 对 `ice.v.ua` 的前端 bundle 进一步逆向后确认：站内存在明确的 API Keys 页面与完整 key 管理逻辑。关键证据包括 `KeysView-BjnUhUwy.js`、`API Keys` 路由 `/keys`，以及前端具备 `create` / `delete` key、展示 `name/key/group/usage/rate_limit/expires_at/status/last_used_at/created_at` 等字段的能力；还内置了将 key 导出为 `OPENAI_API_KEY` / `ANTHROPIC_AUTH_TOKEN` / `GEMINI_API_KEY` 用于 CLI 的说明，证明该站 key 页面是后续“登录后拿真实 key”最优落点。
- 当前最明确的后续执行路径是：优先恢复/复用 `ice.v.ua` 登录态 -> 直接进入 `/keys` -> 拉取或创建真实 key -> 用该 key 调 `https://ice.v.ua/v1/models` 做实际可用性验证，再复制同样方法到其他授权公益站。
