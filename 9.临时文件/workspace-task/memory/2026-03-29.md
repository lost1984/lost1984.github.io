## 2026-03-29 持久记忆补充

### Agent 监督规则（新增固化）
- 对 Code 的监督口径固定为 4 项：
  1. 新 commit
  2. 补完后的文件路径
  3. 可直接执行的命令
  4. 还没打通的点
- 对 Code 的硬判定规则：
  - 有新 commit = 这轮真实往前
  - 没新 commit = 这轮没真正往前
- 对卡在授权 / 权限 / 外部确认的 agent，不允许闷等；必须主动申请，并说清：
  - 要老板做什么
  - 为什么必须老板做
  - 做完后能立刻闭环什么
- 巡查时要额外检查：未完成事项是否已经主动申请所需授权；如果没有，则该轮监督不合格。

### 本轮监督结论
- Code：最新看到新增 commit `0666ab6`，描述 `Enhance ice sign-in with config and auto mode`，因此判定“这轮真实往前”；但仍未闭环，卡在真实主站 token / 登录态与完全自动登录闭环。
- Code 当前相关文件：
  - `/root/.openclaw/workspace-code/ice_signin.py`
  - `/root/.openclaw/workspace-code/ice_signin.example.json`
- News：未完成“晚间日报 2026年03月28日 + AI 驱动创新索引追加”这单，且最新巡查中表现为等待新输入，没有继续主动闭环，也没有按规则主动申请缺失条件，因此本轮判定为“未完且等待，监督不合格，应催办”。

### 用户偏好 / 工作方式
- 用户要的是持续监督其他 agent，重点区分“在干活”与“在等待/卡住”。
- 用户不接受模糊进展汇报，尤其对 Code 只认硬结果；对 News 不接受“继续处理”空话。
- 当前环境无法在 Feishu 中真实后台定时自动巡检，只能在用户消息触发时执行巡查；不能对用户假承诺自动轮询。

## 今日关键监督结论（15:43-17:21）

### Code 当前判定
- 新 commit：`0666ab6`（`Enhance ice sign-in with config and auto mode`）
- 但此后**没有更新的 commit**
- 按硬规则：无新 commit = 这轮没往前
- 当前相关文件：
  - `/root/.openclaw/workspace-code/ice_signin.py`
  - `/root/.openclaw/workspace-code/ice_signin.example.json`
- 卡点仍是：真实主站 token / 登录态 / 完全自动登录闭环
- 验收标准（固化）：
  1. 新 commit
  2. 补完后的文件路径
  3. 可直接执行的命令
  4. 还没打通的点

### News 当前判定
- 未完成《晚间日报 2026年03月28日》飞书文档创建
- 未完成《AI 驱动创新》索引追加
- 基本处于停住状态
- 监督结论：**不合格**
- 后续催办方向：
  1. 晚报文档到底建没建成
  2. 索引到底追加没追加
  3. 如果没完成，缺什么，不允许空等

### Life 侧已确认能力
- **可以创建飞书文档**（feishu-create-doc skill 可用）
- 如果用户直接说"直接创建晚报"，可立即执行
- 当前唯一需要确认的信息：创建位置（默认个人空间根目录）

### 监督规则（固化）
- Code：无新 commit = 这轮没往前
- News：不允许"继续处理"空话，必须给明确状态或兜底交付
- 所有卡授权的对象：必须主动申请，不允许闷等
- 本轮巡查结论：Code 和 News 均未完成，均需催办

