- 用户明确要求：遇到问题时先尝试解决，不要直接把卡点回复给他；默认执行顺序为先自查、先验证、先尝试替代方案、先闭环，只有缺权限/缺关键输入/高风险确认时才打断用户。
- 本次复盘确认：日报任务失败的根因不是环境缺 Python，而是执行判断失误——使用了 `python` 而非 `python3`，且失败后未继续 fallback 到替代链路或文档工具。
- 已在工作区新增兜底流程文件 `intel/daily_brief_fallback.md`，用于约束早晚日报类任务必须先保正文和文档链接，再补索引，不得因单点失败整单回退。
- 用户同意将“先解决、后汇报；能闭环就不甩半成品”作为固定工作偏好记住。
- 飞书用户授权可能按能力域分开触发：文档相关与多维表格（bitable）索引补录不一定共用同一次授权，执行时要预留这一层。
- 当前用户账号下已探测到可访问 bitable：`AI 待办事项管理`（token: `KQjIbq9yKaUhumsMsMZcsf2WnJg`）；暂未发现用户口述的《AI 驱动创新》索引表入口，后续补索引时需要先确认其所在 bitable/app 或表名。
